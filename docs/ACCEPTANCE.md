# 验收标准

## Phase 0：安全开发环境

- [x] 当前工作区是独立 Git 仓库，默认分支为 `main`。
- [x] 存在 `task-companion/`、`test-vault/`、`docs/`、`reference/`。
- [x] 插件骨架可追溯到 Obsidian 官方 Sample Plugin。
- [x] 根目录存在适用于本项目的 `AGENTS.md`。
- [x] 全部指定规划文档均存在并包含第一版内容。
- [x] `package.json` 提供 `build`、`typecheck`、`lint`、`test`。
- [x] `npm run build` 成功。
- [x] `npm run typecheck` 成功。
- [x] `npm run lint` 成功。
- [x] `npm test` 成功。
- [x] 插件源码不含计时器、周期轮询、全局事件监听、遥测或网络请求（Phase 0 要求）。
- [x] 仓库不含真实任务或正式 Vault 内容。
- [x] `test-vault/` 明确标记为仅允许人造测试数据。
- [x] 用户确认 Phase 0，授权进入 Phase 1。

## Phase 1：插件骨架与工程基线

- [x] 插件可构建。
- [x] 插件能在测试 Vault 启用。
- [x] 禁用后没有残留事件或 DOM。
- [x] 设置能够保存。
- [x] 测试命令可执行。
- [x] `typecheck`、`lint`、`test`、`build` 全通过。
- [x] 目录分层与架构文档一致。
- [x] 没有业务功能提前混入。

## Phase 2：可靠计时器与 status 嵌入组件

- [x] Core 层以纯 TypeScript 实现 `idle`、`running`、`paused`、`finished` 状态机。
- [x] 支持 25 分钟、50 分钟和用户输入的自由时长，以及开始、暂停、继续、正常完成、提前结束、重置。
- [x] 运行态使用绝对结束时间计算剩余秒数；暂停态显式保存 `remainingSeconds`。
- [x] 运行态或暂停态拒绝重复启动，同一时间最多一个活动会话。
- [x] 短期计时状态与插件设置共同保存到插件 `data.json`，无 Vault 内容读写。
- [x] Obsidian 重载后恢复计时；若绝对结束时间已过，则恢复为正常完成。
- [ ] 可选系统通知和声音提醒失败时只记录错误，不影响核心状态（待实现）。
- [x] 注册 `taskcompanion` 代码块，且只接受 `view: status`。
- [x] status 空闲时显示 `25:00` 和"任务空闲中"，运行时显示真实剩余时间，暂停时显示"已暂停"。
- [x] 多个 status 实例共享一个计时服务，并每秒只更新各自的文本节点，不重渲染笔记。
- [x] 提供计时控制 Modal，且不包含真实任务选择。
- [x] 单元测试覆盖开始、暂停、继续、结束、跨重载恢复和重复启动保护。
- [x] `npm run typecheck`、`npm run lint`、`npm test`、`npm run build` 全部通过。
- [ ] 仅使用仓库内 `test-vault/` 进行人工验收（待用户操作）。

Phase 2 不包含 Tasks 解析、母任务、子任务、执行记录、复盘、主页集成、网络请求或外部服务。

## Phase 3：正式任务识别与任务选择（已完成）

- [x] 只识别带 `⏫`、`🔼`、`🔽`、`⏬` 或 `🔁` 的未完成 Markdown Tasks。
- [x] 今日待办覆盖 `TASK_RULES.md` 的七条日期规则，逾期任务有明确标识。
- [x] 所有活动的 `⏫` 任务均进入重点任务，不受日期限制。
- [x] 今日＋重点任务去重并标注双重归属。
- [x] 日常循环任务可见，但是否进入今日仍由日期规则决定。
- [x] 首次安全扫描为正式任务追加唯一 `^tc-xxxxxx` 块 ID。
- [x] 已有有效 ID 保持不变；其他块 ID、重复 ID、内容冲突或写入失败时原文不变。
- [x] 任务选择 Modal 支持搜索、分类、日期、来源显示和来源跳转。
- [x] 选择任务后绑定稳定 taskId 并打开现有计时控制 Modal。
- [x] 活动计时期间拒绝切换母任务。
- [x] 扫描失败只显示汇总，不泄露任务正文，不阻断其他文件。
- [x] 不实现 ExecutionSession、子任务、复盘、主页连接、网络或遥测。
- [x] `typecheck`、`lint`、`test`、`build` 全部通过。
- [x] 仅使用仓库内 `test-vault/` 完成人工验收。

## Phase 4：执行会话（已完成并通过人工验收）

- [x] 每次 25/50/自由计时或快速推进生成唯一 ExecutionSession。
- [x] 自动记录 sessionId、taskId、起止时间、活动/暂停秒数、mode、status 和提前结束。
- [x] 结束表单支持完成内容、下一步和可选阻塞；跳过或关闭仍保存基础会话。
- [x] 最新非空 nextAction 按稳定 taskId 回显。
- [x] 长期会话保存在 Vault 的按月 JSONL，而非仅 data.json。
- [x] 读取器隔离坏行并支持旧 schema 迁移；重试按 sessionId 幂等。
- [x] 写入失败保留完整待写队列、显示提示并可运行重试命令。
- [x] 提供当前任务最近 20 条的最小历史查看。
- [x] 测试覆盖会话、时间统计、写入失败和多任务隔离。
- [x] build、typecheck、lint、unit tests 和测试 Vault 产物校验全部通过。
- [x] 仅在仓库测试 Vault 完成人工验收。
- [x] 未实现子任务、正式复盘或主页接入。

## Phase 5：一层任务拆解（已完成并通过人工验收）

- [x] 可直接推进母任务，也可选择一个活动子任务执行。
- [x] 支持子任务添加、改名、稳定排序、完成、取消、返工和设为当前下一步。
- [x] 字段包含 subtaskId、taskId、title、status、order、origin、createdAt、completedAt，并补充 updatedAt/cancelledAt。
- [x] origin 区分最初创建、执行中新增，并保留模板来源读取值但不实现模板界面。
- [x] 计时与快速推进会话固定绑定母任务或一个子任务。
- [x] 自动统计完成数、每个子任务时间、母任务直接投入、总次数与总时间。
- [x] 没有子任务时不显示百分比或虚假进度。
- [x] 子任务变化保存在 Vault 追加式 JSONL，完成和取消历史均保留。
- [x] 测试覆盖母任务隔离、顺序、完成/取消/返工、当前下一步、会话绑定和时间统计。
- [x] build、typecheck、lint、unit tests 和测试 Vault 校验全部通过。
- [x] 未实现多层树、依赖、甘特图、AI、模板界面、正式复盘或主页接入。
- [x] 仅在仓库测试 Vault 完成人工验收。

## Phase 6：任务完成分流与复盘（已完成并通过人工验收）

- [x] 用户明确完成母任务后生成 `reviewStatus: pending`；没有执行档案时自动统计允许为零。
- [x] 用户明确完成子任务后生成独立 `reviewStatus: pending`。
- [x] 原任务完成后立即退出今日、重点和日常活动任务扫描。
- [x] 未完成子任务必须选择返回继续、取消剩余或保留记录并完成。
- [x] 待复盘队列可打开原来源、开始复盘，并列出已完成复盘。
- [x] 自动统计任务跨度、执行天数、会话、有效/暂停时间、提前结束、子任务变化、最长步骤、最后进展和未完成项。
- [x] 支持一段自由复盘和三个可选引导问题，全部允许留空。
- [x] 保存后追加 `reviewStatus: completed`，并生成 Vault 内可读 Markdown。
- [x] 复盘 Markdown 或索引写入失败不回滚原任务，完整内容保留待写并可重试。
- [x] 已完成复盘可以从队列重新打开。
- [x] 测试覆盖分流、统计、未完成子任务、稳定定位和复盘写入失败。
- [x] `typecheck`、`lint`、`test`、production build 和测试 Vault 安装校验全部通过。
- [x] 仅使用仓库内 `test-vault/` 完成人工验收。
- [x] 未实现模板、AI、正式主页或 Phase 8 经验沉淀。

## Phase 7：嵌入式组件与主页副本（已完成并通过人工验收）

- [x] 只注册一个 `taskcompanion` Markdown 代码块处理器。
- [x] 保留空配置和 `view: status` 的原有计时状态行为。
- [x] 支持 current、today、important、daily 和 review 视图；未知或多余配置安全显示错误。
- [x] today、important 和 daily 严格复用 `TASK_RULES.md` 与稳定 taskId，不建立第二套任务列表。
- [x] current 合并专注状态，显示母任务、可选子任务、内联计时和投入/子任务/执行记录；不显示“当前下一步”。
- [x] 任务选择后始终显示执行准备，可直接推进母任务、选择活动子任务，并在同一窗口新增或管理一层子任务。
- [x] 执行准备不显示“编辑任务拆解”，不打开第二个拆解窗口；每个子任务选择按钮后有更多菜单，列表最下方有“新增一个可执行的子任务”和添加按钮。
- [x] 执行准备的子任务行相对母任务向右缩进，左边缘约从“母任务：”的冒号位置开始；底部新增框与子任务行左右对齐。
- [x] 执行准备的子任务选择框和新增输入框高度、内部字号一致，均以 40px 新增输入框为基准。
- [x] 新增输入框右侧只显示圆形 `+` 按钮，其外观和交互状态与子任务三点按钮一致，同时保留明确的无障碍标签。
- [x] 子任务选择框和新增输入框左右边界、边框、圆角、背景、阴影及内边距一致；三点与加号按钮占用同一操作列。
- [x] current 明确显示实际执行目标是母任务还是具体子任务。
- [x] current 在任务/目标绑定、计时状态、会话或子任务变化后自动更新，运行时实时显示剩余时间。
- [x] 进行中、已取消和已完成子任务都直接显示“删除子任务”，无需先取消。
- [x] “取消子任务”保留项目、执行时间和复盘历史且不能作为新执行目标；“删除子任务”清除该目标的子任务引用、全部会话投入、待写队列、子任务复盘索引和可读 Markdown。
- [x] 删除正在运行或暂停计时的子任务会立即重置计时、清除目标绑定且不产生结束会话或收尾弹窗。
- [x] 永久删除不影响其他任务或兄弟子任务，并保留无法解析的 JSONL 行；清理失败不报告成功且保留子任务供重试。
- [x] 任务列表标题直接进入执行准备；标题前使用未选复选框完成，标题旁只保留 `🔗` 来源，不再显示日期/路径副标题。
- [x] 来源优先定位到具体任务行，编辑器定位不可用时安全退化到来源页。
- [x] 拆解页顶部显示具体母任务名；执行准备使用“母任务/子任务：具体标题”的清晰层级。
- [x] 所有 Task Companion Modal 都在 `X` 左侧提供返回按钮；有上一层时恢复刷新后的上一层，根层时返回原页面，`X` 只关闭。
- [x] 当前任务选择器按今日/重点/日常显示母任务，活动子任务默认折叠并按需展开。
- [x] 点击时间开始/暂停/继续，`⚙️` 设置 25/50/自由时长，`⏹` 结束，`↺` 重置。
- [x] 点击结束计时后只更新现有时间与指标节点，当前任务卡不得进入加载态、清空 DOM 或出现整卡闪烁。
- [x] 当前任务卡不显示刷新按钮；其他依赖显式 Markdown 重扫的组件仍保留刷新按钮。
- [x] 当前任务紫色框中的母任务标题与提醒卡标题进入同一个执行准备窗口，可选择母任务或活动子任务；选择子任务后紫色框自动显示母任务、子任务两行。
- [x] 当前任务紫色框只从母任务标题移除 Tasks 日期标记；今日/重点/日常提醒卡继续显示完整日期。
- [x] 当前任务紫色框内的母任务与子任务标题统一为 `0.85rem`，大于层级标签且小于旧版子任务的 `1rem`。
- [x] 当前任务紫色框内母任务和子任务标题在共用标题列中左对齐；母任务右侧图标使用独立固定列，不破坏上下对齐。
- [x] `🔗` 打开来源，完成按钮安全完成当前所选活动子任务或母任务；执行记录指标可打开历史。
- [x] 当前任务显示活动子任务时，子任务行也有完成按钮，且与母任务完成按钮纵向对齐；两者分别完成子任务和母任务，不互相替代。
- [x] 已完成子任务生成独立待复盘项；未完成或返工的 pending 子任务不显示，且不会重复生成。
- [x] 复盘列表和主页复盘卡不显示日期/来源路径副标题。
- [x] 直接完成在未完成子任务分流结束后才报告成功，并同步刷新同页任务组件。
- [x] 拆解界面的每个子任务只占一行，不显示统计摘要、说明副标题或横向多按钮阵列。
- [x] 子任务标题支持失焦/回车保存；排序、下一步、完成、取消、恢复和删除集中在原生更多菜单。
- [x] 全部 Modal 的返回与关闭按钮具有一致尺寸、圆形外观、对齐和悬停/按压反馈。
- [x] 来源、完成、计时、展开、返回和更多操作统一使用 Lucide 线性图标，不混用 Emoji 图标。
- [x] Modal、主页卡片、表单、计时和层级列表共享统一视觉变量，并适配窄窗口与减少动态效果设置。
- [x] current 按全部活动正式任务读取，跨日退出可见栏目后仍保持显示。
- [x] review 显示 pending/completed 数量、最近待复盘项和复盘队列入口。
- [x] 任务卡支持打开来源和调用既有选择/计时流程；不复制计时或完成逻辑。
- [x] 每个数据组件提供显式刷新及明确的加载、空、部分失败状态。
- [x] 同一主页同时加载的任务组件合并进行中的安全扫描，避免稳定 ID 写入竞争。
- [x] MarkdownRenderChild 卸载时清理计时订阅、DOM 事件和异步渲染，不残留后台工作。
- [x] current 卸载时同时取消计时、会话和子任务服务订阅。
- [x] `test-vault/Task Companion Home.md` 使用人造数据组合全部组件。
- [x] 样式使用 Obsidian CSS 变量并在窄窗口下可读，不修改正式主页或主题文件。
- [x] 结束/自然完成计时只保存 ExecutionSession，不弹出“本次执行已结束”，也不产生复盘资格。
- [x] 当前任务卡确认完成目标后弹出“本次执行已结束”，支持本次完成、下一步和注意事项，文字可跳过但基础完成记录仍保存。
- [x] pending 复盘项显示已选复选框；取消勾选可按稳定 ID 恢复母任务为 `[ ]` 或子任务为 `active`，并使其回到活动提醒/执行流程。
- [x] 重新完成曾撤销的目标复用原 pending reviewId；已保存 completed 复盘历史不因返工删除，明确永久删除该子任务时例外。
- [x] 完成/撤销、计时静默保存、复选框结构和注意事项文案均有自动回归测试。
- [x] 测试覆盖配置解析、任务分组、并发扫描、结构清理和主页副本代码块。
- [x] build、typecheck、lint、unit tests 和测试 Vault 产物校验全部通过。
- [x] 仅在仓库测试 Vault 完成人工验收。
- [x] 不实现正式主页安装、后台监听、模板、AI 或 Phase 8 经验沉淀。

## Phase 8：经验沉淀与扩展接口（已完成并通过人工验收）

- [x] 母任务复盘成功保存后提供新建模板、更新已有模板和暂不沉淀。
- [x] 模板保存有序子任务标题、历史平均执行次数/投入、常见阻塞、完成前检查和下次沿用经验。
- [x] 模板事件写入 Vault 内追加式 JSONL，按 eventId 幂等，坏行隔离并按模板版本折叠。
- [x] 新任务只获得模板建议；只有用户明确采用后才原子追加缺失子任务，已有标题不重复。
- [x] 模板建议显示步骤、平均执行次数/时间、完成前检查和沿用经验。
- [x] 插件实例最初公开 `apiVersion: 1.0.0`；后续兼容新增能力升级次版本，破坏性变更必须升级主版本。
- [x] API 提供当前目标、计时、会话、复盘、模板、界面入口和类型化事件订阅。
- [x] 提供 11 个约定生命周期事件，载荷不含任务正文或绝对路径。
- [x] 用户扩展目录只接受 schemaVersion 1 声明式 JSON；首次发现默认停用。
- [x] 受控权限只允许通知和打开既有视图，不提供网络、Shell、任意 JavaScript 或任意 Vault 写入。
- [x] 扩展失败记录脱敏错误并回退上一有效版本；没有旧版本时自动停用，核心流程继续运行。
- [x] 复盘组件“今日已完成”按本地完成日期过滤并按稳定目标去重，点击同形指标打开今日队列。
- [x] “今日已完成”指标外观与“待复盘”一致；仅可点击文字悬停变紫并显示手型指针。
- [x] 主页移除“打开完整复盘队列”按钮，命令面板完整队列入口保留。
- [x] 自动测试覆盖模板聚合、建议排序、原子应用、默认停用、权限拒绝、错误隔离和版本回退。
- [x] production build、typecheck、零警告 lint、73 个单元/结构测试和 2 个测试 Vault 校验通过。
- [x] 仅在仓库测试 Vault 完成人工验收。
- [x] 未实现脚本市场、AI 自动修改、多层子任务或正式 Vault 安装。

## Phase 9：稳定测试、发布构建与正式安装（进行中）

- [x] 检查 Git 状态并建立独立 `codex/phase-9-release` 分支。
- [x] 阶段 0–8 功能范围冻结；发布阶段不新增业务能力。
- [x] manifest、package、lockfile 与 versions 使用一致的确认版本和作者信息。
- [x] 发布构建可复现，且安装包只包含 `main.js`、`manifest.json`、`styles.css`。
- [x] 发布产物生成 SHA-256 校验和，不包含 source map、`data.json`、测试数据、日志或个人文件。
- [x] 自动测试覆盖版本一致性、发布白名单、压缩包结构和禁止项。
- [x] 文档说明全新安装、覆盖升级、数据备份、失败回滚、卸载和恢复步骤。
- [x] 在仓库 `test-vault/` 完成候选安装、覆盖校验、禁用/启用、Obsidian 重载和主要功能回归；正式覆盖前另有完整回滚备份。
- [x] production build、typecheck、零警告 lint、74 项核心测试、4 项发布测试和 3 项测试 Vault 校验通过。

## Phase 10：测试主页四类提醒统一（进行中）

- [x] 插件主页快照包含 `daily`、`today`、`important`、`pending` 四组，组内按主页既有规则排序。
- [x] 今日待办覆盖逾期 Due、今日 Due、今日或过去 Scheduled、今日 Start、Start–Due 覆盖今天、只有 Start 且已开始。
- [x] 日常任务按显式日期、最近完成日期和 recurrence 规则判断当前是否可用；早起、reading、运动只接受今日日记来源。
- [x] 重点任务包含全部活动 `⏫`；待推进任务只包含非今日、非循环、非 `⏫` 的其他正式任务。
- [x] API 返回稳定 ID、显示标题、Vault 相对来源、行号、日期、优先级、循环与今日状态，不返回绝对路径。
- [x] `today`、`important`、`daily` 和新增 `pending` 嵌入组件复用同一快照，不重复实现筛选规则。
- [x] `Home/Home-Test.md` 保持现有 DOM 结构、CSS 类名、标签页、指标和排版，只移除本地扫描/筛选/排序并引用插件快照。
- [x] 原主页、移动主页、原 CSS 和其他 Vault 文件无修改。
- [x] `Home-Test` 任务提醒主卡与待推进卡以 400px 为最小高度，任务增加或标题换行时按完整内容自动增高，不使用 `overflow:hidden` 裁切任务。
- [x] 每次 Dataview 内容重建后重新测量三种任务标签终态及待推进列表，字体就绪后再次校准，两侧任务卡保持同高。
- [x] `Home-Test` 原“复盘入口”卡片替换为插件原生 `view: current` 当前任务组件；主页不复制当前任务、计时、母/子目标或指标逻辑。
- [x] 当前任务组件继续位于原右侧卡片位置，外层复用 `hk-side-card`，测试 CSS 适配其宽度、圆角、间距、颜色与窄屏排版。
- [x] Home-Test 将插件当前任务组件移入“今日任务处理中”标题右侧，计时与投入/执行记录位于左列，具体执行目标位于中列，选择任务按钮位于最右列；原右上角三项任务数量指标和独立当前任务卡移除。
- [x] 待推进卡标题右侧显示插件快照中的具体项数，字号与可折叠任务栏数量一致，卡片仍按内容自然高度显示。
- [x] 当前任务摘要在宽屏下分为左右两组：左组为计时及投入/执行记录，右组为母任务及子任务；选择任务按钮位于母任务区域右上角，整体高度由组件内容与紧凑间距自然决定。
- [x] 当前任务摘要最终在宽屏下为三列：计时、投入时间/选择任务、母任务/子任务；不显示执行记录与次数，计时数字位于暂停状态上方，目标框及摘要整体使用更紧凑的自然高度。
- [x] “今日任务”标题右移并居中；计时数字放大、状态同宽居中、三个计时按钮纵向排列；投入时间与选择任务等宽等字号；母任务和子任务的完成按钮位于同一垂直线上。
- [x] 倒计时运行时“正在专注 · 点击暂停”保持单行；计时数字使用更大字号；母任务和子任务等分目标框高度；累计投入显示为“累计 N分钟”紧凑格式且颜色与选择任务一致。
- [x] 选择任务与计时数字悬停时文字颜色、字重和鼠标指针均有明确变化，键盘聚焦提供同等反馈；待推进卡与主任务卡取双方完整内容所需的最大高度并保持上下对齐。
- [x] Home-Test 的任务复盘卡使用插件原生 `view: review`，位于“学习”入口卡右侧空位并保持顶部对齐；宽度不足时不使用固定偏移，按响应式侧栏自然排列。
- [x] 所有 `hasRecurrence` 的日常任务不出现在任何任务选择器中，但仍可出现在“日常任务”提醒；非循环的今日、重点和待推进任务仍可选择。
- [x] 正式安装只覆盖 `main.js` 和 `styles.css`；两者与发布构建逐字节一致，manifest、插件设置和 `TaskCompanion/` 长期数据与安装前备份一致。
- [x] 当前任务组件的计时设置包含“确定”按钮；自由时长可通过按钮或 Enter 提交，非法值不会保存，保存期间不能重复提交，成功后刷新计时显示并关闭设置框。
- [x] 当前任务来源失效或尚未选择任务时，主页当前任务区域仍保留计时数值、计时设置与控制按钮、累计投入和“选择任务”入口；仅任务标题区显示降级提示，且不得与其他控件重叠。
- [x] Home-Task 宽屏标题“今日任务”在其左侧空白栏内视觉居中，不因当前任务摘要宽度而向右偏移。
- [x] 当前任务母任务/子任务标题框严格收敛在主页主卡内容宽度内，标题可换行且操作按钮不得伸入右侧栏。
- [x] 日常任务、今日待办和重点任务三个可伸缩任务栏在每个选中状态下共享同一底边，窄栏只改变宽度和内容密度，不改变外框高度。
- [x] Home-Task 当前任务三框组合总宽度不变并整体略向左移；计时框减少的宽度完整转移给母任务/子任务框，中间累计投入与选择任务列宽不变，窄屏不产生横向偏移。
- [x] Home-Task 顶部计时列与母/子任务列在桌面窗口变化时按固定比例共同伸缩，计时列比上一版更窄；中间操作列固定，整体宽度与左移位置不变，最小宽度下不溢出。
- [x] 三个可伸缩任务栏在任务内容或可用宽度变化后遍历全部展开状态计算最大自然高度，并把该值同时锁定到三栏；切换展开栏时高度保持不变，下一次内容或宽度变化才重新计算。
- [x] 三个任务栏统一高度确定后，其下边缘到主任务卡下边缘的距离等于母任务/子任务框上边缘到主任务卡上边缘的距离；若主卡最低高度或待推进内容要求额外高度，额外空间进入三个任务栏而不是底部留白，主任务卡与待推进卡始终等高。
- [x] Home-Task 当前任务中间列的累计时间框与选择任务框统一加宽；累计框顶部和选择框底部分别向内收相同距离，两端留白对称，但不要求与计时框及母/子任务框上下边平齐。
- [x] 三个等高任务栏在任意窗口缩放比例和宽度动画终态下完整显示下边框与圆角；高度测量保留亚像素安全余量，内部底边描线不覆盖任务内容，也不改变上下对称留白和两侧外卡等高。
- [x] 三个任务栏使用位于边界内侧的独立下边与圆角覆盖层，不依赖可能落在锁定高度边界上的原始边框或内阴影；普通栏和活动栏均完整可见。
- [x] Home-Task 顶部扩大“今日任务”标题列并取消当前任务摘要左移，母/子任务框随摘要区域缩窄；其最右边缘与下方“重点任务”切换按钮最右边缘严格对齐。
- [x] Home-Task 三任务栏恢复 `Home-kanban` 的 flex 填充与真实末项高度测量方式：不设置单栏固定像素高度、不绘制额外底边覆盖层，三栏由任务面板自然拉伸并完整保留原边框、圆角和阴影；主卡与待推进卡继续等高且不覆盖后续内容。
- [x] Home-Task 顶部使用共享四列网格：标题列与计时列宽度完全相等且均至少 150px，中间列固定，计时列与母/子任务列按 0.38:1.68 分配剩余宽度；母/子任务框右边与重点任务切换按钮右边严格同线。
- [x] `home-kanban-test.css` 的全部视觉规则仅在 Home-Task 独有的 `home-task-test` 页面类下生效，不得命中 `Home-kanban` 或其他主页；“今日任务”标题保持单行并获得与计时框接近的适度留白宽度。
- [x] build、typecheck、零警告 lint、79 项单元/结构测试、4 项发布测试和 3 项测试 Vault 校验通过。
- [ ] Obsidian 实际呈现等待下次自然重载确认；按用户要求不进行前台操作。
- [x] 正式版测试 Modal 不再显示 Phase 1/skeleton 占位文案，并有自动回归测试。
- [x] 用户在仓库测试 Vault 完成发布候选人工验收。
- [x] 正式安装前明确目标 Vault、旧状态、插件目录和主页范围；初次安装不修改主页，后续主页显示修正另行获得明确授权并先备份。
- [x] 正式安装的三个文件与 1.0.0 候选产物逐字节一致，SHA-256 匹配。
- [x] 正式安装后插件启用、测试 Modal 和正式任务扫描入口验证通过；同一候选包的计时、会话、子任务、复盘、模板及清理已在测试 Vault 完整验收，回滚备份可用。
- [x] 用户确认 Phase 9 正式安装验收通过。
- [x] 未经授权不提交、不打标签、不合并 main、不对外发布。
- [x] 正式桌面/移动主页只在 `cleanTaskText` 中过滤标题末尾 `^tc-xxxxxx`；稳定 ID、任务内容和其他主页逻辑保持不变。

## Phase 11：倒计时提醒与自动番茄循环（进行中）

- [x] 25 分钟专注自然归零时弹出“专注完成”窗口并播放本地提示音。
- [x] Obsidian 不在前台时尝试发送桌面系统通知；系统权限拒绝不得中断计时、会话或下一阶段。
- [x] 暂停和手动提前结束不播放声音、不弹完成提醒、不自动开始下一阶段。
- [x] 第 1、2 次 25 分钟专注后准备 5 分钟休息，第 3 次后准备 15 分钟休息；第 4、5 次恢复 5 分钟，第 6 次再次为 15 分钟，并持续循环。
- [x] 专注或休息自然结束后先提醒并停留在下一阶段待开始状态，只有用户再次点击开始后才运行倒计时。
- [x] 休息计时保留当前母/子任务绑定，但不生成 ExecutionSession、不增加累计投入或复盘资格。
- [x] 自动休息用途和当前计时可持久化；旧版计时状态恢复时默认视为专注计时。
- [x] 自动测试覆盖轮次函数、休息会话隔离、自然/提前结束分流、持久化兼容和提醒接线。
- [x] 当前任务累计投入始终显示总分钟数“XX分钟”，超过 60 分钟也不换算为小时。
- [x] build、typecheck、零警告 lint、全部测试、发布构建和测试 Vault 校验通过。
- [x] `1.1.1` 发布、正式 Vault 备份升级及安装产物/长期数据/主页不变核验完成；安装期间新启动的计时状态原样保留。
