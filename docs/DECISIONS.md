# 决策记录

## 2026-07-16：Phase 2 可靠计时器

- 用户明确批准 Phase 2 仅实现可靠计时器与 `status` 嵌入组件。
- 自由计时定义为用户输入分钟数的自定义倒计时，以保持“剩余时间”语义一致。
- 核心状态机使用绝对毫秒时间戳，不依赖 tick 次数累计时间。
- 暂停态显式持久化剩余秒数；重载不消耗暂停时长。
- 同时最多一个运行或暂停会话；重复启动必须被拒绝。
- 计时短期状态仅保存到插件 `data.json`，不写入笔记。
- 系统通知和声音是可选外围能力，失败不得改变核心计时状态。
- 本阶段不实现真实任务选择、Tasks 解析、任务层级、执行记录、复盘和主页集成。

## 2026-07-16：Phase 3 正式任务识别与任务选择重做

- 以用户确认完成的 `v0.2.0-timer` 为唯一基线，不复用未验收的 Phase 3/4 后台实现。
- 正式任务、今日、重点和日常规则以 `TASK_RULES.md` 为准。
- 长期任务身份采用行末 `^tc-xxxxxx` 块 ID；路径和行号仅作本次扫描定位。
- 扫描只由任务选择器打开或刷新显式触发，不注册后台 Vault 监听。
- 只允许追加稳定 ID；完整文件或目标原行变化时停止写入。
- 当前选择的 `taskId` 属于短期运行状态，可与计时状态一起保存到 `data.json`。
- 使用官方 `Vault.process`，最低 Obsidian 版本提升到 1.1.0。
- Phase 3 不实现 ExecutionSession、子任务、复盘或主页接入。
- 旧的 `v0.3.0-task-selection` 保留为历史错误实现；人工验收通过的重做版本使用 `v0.3.1-task-selection`，不改写旧标签。
