# 当前计划

## 本轮范围

本轮只完成 Phase 2：可靠计时器与 `status` 嵌入组件。用户已明确批准本阶段计时需求；仍不允许真实任务选择、任务解析、任务层级、执行记录、复盘或主页集成。

## 执行清单

- [x] 读取项目约束和全部现有长期文档；确认 `docs/DECISIONS.md` 缺失。
- [x] 检查 Git 状态；确认 `phase-2-timer` 与 `main` 从同一提交起步。
- [x] 输出 TimerState 状态机方案和预计修改文件。
- [x] 先补 Phase 2 验收标准。
- [ ] 为纯 TypeScript 状态机补失败测试。
- [ ] 实现计时状态机、序列化校验和跨重载恢复。
- [ ] 实现单例计时服务、持久化和隔离失败的提醒服务。
- [ ] 实现 `taskcompanion` status 代码块和计时控制 Modal。
- [ ] 运行 typecheck、lint、tests、build。
- [ ] 更新全部相关长期文档并给出手工验收步骤。
- [ ] 停止并等待用户验收；不合并 `main`。

## 明确不实现

- Tasks/Markdown task 解析。
- 任务选择、子任务或任务状态变更。
- 执行日志、复盘或主页接入。
- Vault 内容扫描、读取或写入。
- 网络请求、遥测、外部服务或正式发布。

## 待确认问题

1. 插件 ID、作者、最低 Obsidian 版本和移动端支持范围何时最终确定？
2. `showTechnicalDetails` 是否只作为骨架测试设置，后续可以删除？本轮保留以避免无关迁移。

其余产品问题仍以 Phase 0 问题清单为背景，未经确认不得实现。

## 停止条件

全部自动检查和文档更新完成后立即停止。不得合并 `main`，不得开始 Phase 3，等待人工验收。
