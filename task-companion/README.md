# Task Companion Plugin Skeleton

这是基于 Obsidian 官方 Sample Plugin 建立的最小工程骨架。官方示例中的命令、通知、设置、编辑操作、全局事件和定时器均已移除；当前入口不实现正式功能。

## Commands

```bash
npm ci
npm run build
npm run typecheck
npm run lint
npm test
```

构建生成的 `main.js` 只用于本地验证，不纳入 Git。

